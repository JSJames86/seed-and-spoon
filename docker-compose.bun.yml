version: '3.8'

services:
  # Next.js frontend with Bun
  nextjs:
    build:
      context: .
      dockerfile: Dockerfile.bun
    container_name: seedspoon-nextjs
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: 1
    env_file:
      - .env
