[install]
# Use default npm registry
registry = "https://registry.npmjs.org/"

# Cache configuration
cache = true

# Production mode
production = false

# Retry configuration for network issues
retry = 5

[install.scopes]
# Default scope uses npm registry
"@" = "https://registry.npmjs.org/"
