export const metadata = {
  title: 'Links',
  description: 'All the ways to support and connect with Seed & Spoon.',
  robots: { index: false, follow: false },
};

export default function LinksLayout({ children }) {
  return children;
}
