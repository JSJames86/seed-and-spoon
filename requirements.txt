# ==============================================================================
# CORE BACKEND DEPENDENCIES
# ==============================================================================
Django==5.0.1
djangorestframework==3.14.0
psycopg2-binary==2.9.9  # PostgreSQL adapter
stripe==8.2.0
django-environ==0.11.2  # Environment variable management

# ==============================================================================
# AUTHENTICATION & SECURITY
# ==============================================================================
djangorestframework-simplejwt==5.3.1  # JWT authentication for DRF
django-cors-headers==4.3.1  # CORS support for frontend

# ==============================================================================
# GEOSPATIAL SUPPORT (PostGIS)
# ==============================================================================
# Note: Requires system-level GDAL, GEOS, and PROJ libraries installed
# On Ubuntu/Debian: apt-get install gdal-bin libgdal-dev libgeos-dev libproj-dev
# On macOS: brew install gdal geos proj
# Uncomment the line below after installing system dependencies:
# django==5.0.1  # Already included above; django.contrib.gis is built-in

# ==============================================================================
# ASYNC & BACKGROUND TASKS
# ==============================================================================
celery==5.3.6
redis==5.0.1
django-celery-beat==2.6.0  # Periodic task scheduler (updated for Django 5.0 support)
django-celery-results==2.5.1  # Store task results in Django DB

# ==============================================================================
# DEPLOYMENT & PRODUCTION
# ==============================================================================
gunicorn==21.2.0  # WSGI HTTP Server
whitenoise==6.6.0  # Static file serving
dj-database-url==2.1.0  # Database URL parsing (useful for deployment)

# ==============================================================================
# DEVELOPMENT TOOLS
# ==============================================================================
black==24.1.1  # Code formatter
isort==5.13.2  # Import sorter
flake8==7.0.0  # Linting
mypy==1.8.0  # Type checking
django-stubs==4.2.7  # Django type stubs for mypy
djangorestframework-stubs==3.14.5  # DRF type stubs for mypy

# ==============================================================================
# TESTING
# ==============================================================================
pytest==8.0.0
pytest-django==4.7.0
pytest-cov==4.1.0  # Coverage reporting
factory-boy==3.3.0  # Test fixtures
faker==22.5.1  # Fake data generation for tests

# ==============================================================================
# UTILITIES
# ==============================================================================
python-dotenv==1.0.1  # Alternative to django-environ (both included for flexibility)
pillow==10.2.0  # Image processing (useful for user uploads)
